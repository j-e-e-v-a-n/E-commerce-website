{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "user.js"
    }
  ],
  "scripts": {
    "build": "npm install && npm run build",
    "start": "npm start"
  }
}
